import React from "react";
import DynamicGridWidget from "./DynamicGridWidget";

const SalesWidget: React.FC = () => {
    return (
        <DynamicGridWidget structureName="영업관리_MTBL" />
    );
};

export default SalesWidget;
