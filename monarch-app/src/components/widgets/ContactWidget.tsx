import React from "react";
import DynamicGridWidget from "./DynamicGridWidget";

const ContactWidget: React.FC = () => {
    return (
        <DynamicGridWidget structureName="접촉관리_MTBL" />
    );
};

export default ContactWidget;
