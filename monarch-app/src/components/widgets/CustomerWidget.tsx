import React from "react";
import DynamicGridWidget from "./DynamicGridWidget";

const CustomerWidget: React.FC = () => {
    return (
        <DynamicGridWidget structureName="고객관리_MTBL" />
    );
};

export default CustomerWidget;
